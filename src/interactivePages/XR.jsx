import React from 'react'
import {Route, BrowserRouter as Router, Routes, Outlet} from 'react-router-dom';

const XR = () => {

  return (
    <div> 
        <Outlet />
    </div>
  )
}

export default XR